

// You can't detect them as identifiers.
// Compromise; let identifer color be Normal.


// http://yuroyoro.hatenablog.com/entry/20091026/1256540286
import java.lang.{String ⇒ 文字列}

val どんな空気よ_? = (なぜか変換できない : List[String]) ⇒ 
  for( ふいんき ← なぜか変換できない ) { 
    println( ふいんき )
  }
どんな空気よ_?(List( "よい" ,"悪い"))

val m = Map( "日本語で書いても読みにくくね?" → "それをいっちゃあおしまいよ" ,
             "誰得?" → "おまえが言うな"  )

object こんにちわ {
    def さようなら(): Unit = ()
    あ_+
    a_+
    +++
    'あ'
}
